{
  "name": "supost-mcp",
  "version": "0.2.1",
  "private": true,
  "description": "Remote MCP server for SUpost, the Stanford student marketplace — search listings, fetch listing details, read market stats, and message posters and create draft listings (messages require an emailed confirmation click; drafts are published by the human at a hand-off URL).",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "check": "npm run typecheck && npm run test"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "1.26.0",
    "mcp-handler": "^1.1.0",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@types/node": "^22.14.0",
    "typescript": "^5.8.0",
    "vitest": "^4.1.10"
  }
}
